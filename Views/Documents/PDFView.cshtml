@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using Syncfusion.EJ2
@{
    ViewBag.Title = "DefaultFunctionalities";
}

    
    <div class="main-content e-responsive" id="main-text">
        <div class="sidebar-content sidebar" style="display:flow-root; min-height:600px;">

            <div class="control-section col-lg-12" style="height: 1000px;">

                <h4 id="titulo_vista" class="h4 tr" key="documentosComercial">VER PDF</h4>
                <hr style="border-width: medium;" />

                <div id="viewpdf" style="width: 100%; height: 100%;"></div>

                @*<div class="control-section">
                    @Html.EJS().PdfViewer("pdfviewer").ServiceUrl(VirtualPathUtility.ToAbsolute("~/ProyectoAlmaInicio/RepositorioAlma/")).DocumentPath("Arquitectura.pdf").Render()
                </div>*@
            </div>
        </div>
    </div>

    <script src="~/Scripts/pdfobject/pdfobject.min.js"></script>
    <script>
        const queryString = window.location.search;
        const urlParams = new URLSearchParams(queryString);
        const archivo = urlParams.get('archivo');

        var titulo_vista = document.getElementById('titulo_vista');
        titulo_vista.innerHTML = 'VER PDF - ' + archivo;

        var rutaPDF = '/ProyectoAlma/RepositorioAlma/' + archivo;
        console.log(rutaPDF);

        var viewer = $('#viewpdf');
        PDFObject.embed(rutaPDF, viewer);
    </script>

    <script>
        $(document).ready(function () {
            console.log('ready');
        });
    </script>

        <style>

            .file {
                width: 100%;
                border: 1px solid #ced4da;
                border-radius: 5px;
                padding: 5px 5px 5px 5px;
            }

            input[type="file"]:focus {
                outline: none;
                border: 3px solid #a6ddba;
                border-radius: 5px;
            }

            p {
                margin: 0 0 0px;
                margin-bottom: 0px;
            }

            .h4 {
                color: black !important;
                font-family: Gotham Medium !important;
            }

            .h5 {
                color: black !important;
                font-family: Gotham Medium !important;
            }

            .main-content {
                background: #F7F7F7 !important;
                font-family: Gotham Medium !important;
            }

            .btn, .btn:focus, .btn:active:focus {
                outline: none;
                background: white;
                padding-top: 0px;
            }

            .btn2 {
                padding: 6px 10px 6px 10px;
                border-radius: 3px;
            }

            .icono {
                color: rgb(74, 185, 115);
            }

                .icono:hover, .icono:focus {
                color: forestgreen;
                }
        </style>